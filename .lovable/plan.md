
The user wants the full implementation of everything discussed since the ElevenLabs Scribe v2 screenshot:

1. Upgrade dictation to `useScribe` from `@elevenlabs/react`
2. Version-aware tool auto-sync (silent, no UI)
3. Push agent app context (knowledge of the app) along with tools in `sync-agent-tools`
4. New "What George can do" capabilities page + sidebar nav entry

Let me confirm the existing files quickly so the plan references real names, then present a concise implementation plan.
